# PDS Configuration Environment Variables

# Admin password for PDS management
PDS_ADMIN_PASSWORD=ResonanceAdmin2025!

# JWT secret for token signing (generate a secure random string)
PDS_JWT_SECRET=resonance-jwt-secret-2025-very-secure-random-string

# Database credentials (should match docker-compose.yml)
POSTGRES_DB=pds
POSTGRES_USER=pds
POSTGRES_PASSWORD=pds_password_2025

# Domain configuration
PDS_HOSTNAME=sync.terasync.app
PDS_USER_DOMAIN=sync.terasync.app

# Contact email for SSL certificates
CADDY_EMAIL=admin@terasync.app